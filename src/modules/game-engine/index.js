const { GameEngine } = require('./game-engine');
const {
    GAME_EVENTS_NAMES,
    NEED_TO_PLAY_AGAIN_ANSWERS,
    POSSABLY_VALUES,
} = require('./game-engine-constants');

module.exports = {
    GameEngine,
    GAME_EVENTS_NAMES,
    NEED_TO_PLAY_AGAIN_ANSWERS,
    POSSABLY_VALUES,
};